services:
  telegram_download_bot:
    image : telegram_download_bot:latest
    container_name: telegram_download_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    
